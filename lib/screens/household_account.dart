import 'package:flutter/material.dart';

class HouseAccount extends StatelessWidget {
  const HouseAccount ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('House'),
    );
  }
}
